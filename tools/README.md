# Maintenance tools

This directory contains scripts that were **disabled** in the supplied ioBroker production export. They are kept separate from `scripts/` so that one-shot migration, cleanup, history and importer utilities cannot be mistaken for continuously running production modules.
